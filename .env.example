# MongoDB Atlas connection (provided by user)
# Replace <db_password> with the real password for user `madi`.
MONGODB_URI=mongodb+srv://madi:<db_password>@cluster0.rnb3q.mongodb.net/diploma-platform?appName=Cluster0

# Example with actual password value from the current request:
# MONGODB_URI=mongodb+srv://madi:madi@cluster0.rnb3q.mongodb.net/diploma-platform?appName=Cluster0

JWT_SECRET=replace_with_strong_secret
PYTHON_RECOMMENDER_URL=
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
